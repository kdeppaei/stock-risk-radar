services:
  - type: web
    name: openkiri
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn openkiri_app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
